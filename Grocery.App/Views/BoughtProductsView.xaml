<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             x:Class="Grocery.App.Views.BoughtProductsView"
             x:DataType="vm:BoughtProductsViewModel"
             Title="BoughtProductsView">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Product Picker -->
        <Picker Grid.Column="0" Grid.Row="0" Margin="10,0,0,0" 
                Title="Klik op onderstaand pijltje om een product te selecteren:"
                ItemsSource="{Binding Products}"
                ItemDisplayBinding="{Binding Name}"
                SelectedItem="{Binding SelectedProduct, Mode=TwoWay}" 
                BackgroundColor="{StaticResource Primary}"
                TextColor="{StaticResource Secondary}" />

        <!-- Resultaat lijst -->
        <Border Grid.Row="1" Grid.Column="0" Stroke="#C49B33" StrokeThickness="4" Padding="5" Margin="5" HorizontalOptions="Center">
            <StackLayout>

                <!-- Statusboodschap altijd boven de lijst -->
                <Label Text="{Binding StatusMessage}"
                       FontSize="18"
                       FontAttributes="Bold"
                       HorizontalTextAlignment="Center"
                       Margin="10" />

                <!-- CollectionView met aankopen -->
                <CollectionView ItemsSource="{Binding BoughtProductsList}" Margin="5" SelectionMode="Single">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="m:BoughtProducts">
                            <Grid Padding="5" ColumnDefinitions="*,*">
                                <!-- Client naam -->
                                <Label Text="{Binding Client.Name}" 
                                       Grid.Column="0"
                                       FontSize="16" 
                                       FontAttributes="Bold" />
                                <!-- Naam boodschappenlijst -->
                                <Label Text="{Binding GroceryList.Name}" 
                                       Grid.Column="1"
                                       FontSize="16" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

            </StackLayout>
        </Border>
    </Grid>
</ContentPage>
